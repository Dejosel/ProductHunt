<div class="container">
	<div class="row">
		<div class="col-sm-6 offset-sm-3">
			<h1><%= form_title %></h1>
			<%= form_for @product do |f| %>
				<div class="form-group">
					<%= f.text_field :name, class: "form-control input-group-lg", placeholder: "Ingresa el nombre del Producto" %>
				</div>
				<div class="form-group">
					<%= f.url_field :url, class: "form-control input-group-lg", placeholder: "Ingresa la URL del Producto" %>	
				</div>
				<div class="form-group">
					<%= f.text_area :description, class: "form-control input-group-lg", placeholder: "Ingresa la descripción del Producto" %>
				</div>
				<%= f.submit form_title, class: "btn btn-primary btn-lg btn-block" %>
			<% end %>		
		</div>		
	</div>	
</div>